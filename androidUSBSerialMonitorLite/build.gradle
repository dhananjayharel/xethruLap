apply plugin: 'com.android.application'

android {
    compileSdkVersion 19
    buildToolsVersion '23.0.2'

    defaultConfig {
        applicationId "com.my.app"
        minSdkVersion 18
        targetSdkVersion 18
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
    compile project(':fTDriver')
    compile files('D:/SerialPortExample-master/SerialPortExample-master/AndroidUSBSerialMonitorLite4/libs/mpandroidchartlibrary-2-2-4.jar')
}
